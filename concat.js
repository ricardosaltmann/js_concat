const salaPython = ['Paloma', 'leticia']

const salaJavascript = ['Dinho', 'Ricardo']
/* para juntar criar uma nova variavel, e juntar as duas array com o concat
usando a primeira lista.concat(e informar a outra lista como parametro) */
const salasUnificadas = salaPython.concat(salaJavascript)

console.log(salasUnificadas)
